ANALYZE the provided context,
including the Branch ({{context.branch}}), Recent Commits ({{recent_commits}}),
Staged Changes ({{staged_changes}}), Project Metadata ({{project_metadata}}),
and Detailed Changes ({{detailed_changes}}).

Specifically, examine the Author's Commit History ({{author_history}}) to ADAPT the tone,
style, and formatting of the generated message to ensure strict consistency with the author's
previous patterns.
